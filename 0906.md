#

没有完全自洽
但是，目前仍然有一个我认为必须再修改的问题:Proposition1仍然
这是现在最值得你们继续修改的一处。
上一版的问题是:
U1= +0.077, 95%CI= \[0.006,0.149]
CI没有包含0, 却声称 late stage 是"statistically indistinguishable from zero”。
这一版已经改进了，引入了practical equivalence margin:
E=0.1dB.
并说:
0.006 < 0.1
因为 late-stage gain"practically negligible”。 final.o9o3.with letter
这比上一版合理很多。
但是，从严格数学逻辑看，还有最后一个不一致。

<br />

目前 Proposition 前半段定义:

$z_k^* = \mathbf{1}[U_k > 0 \text{ and stage } k \text{ admissible}].$

也就是说，如果目标是:

$\max Q,$

那么只要:

$U_l = +0.077 > 0$

并且 late stage admissible，就应该:

$z_l^* = 1.$

也就是应该 high，而不是 conservative。

正文随后又说:

> late stage takes the conservative tie-break

因为它的 gain 小于 practical equivalence threshold。

***

问题在于，这已经不再是单纯:

$\max Q$

而变成了:

> 在 fidelity 近似等价的情况下，再最小化 texture risk。

换句话说，你实际使用的是 **lexicographic / constrained multi-objective criterion**，但是 Proposition 最开始仍然写成"maximizing Q"。

从 PDF 第 12 页的实际排版看，这个逻辑很清楚，数学较强的 Reviewer 很可能能看出来。

`final_0903_with_letter`

### 我建议做一个很小但很关键的修改

不用删掉 CAI，也不用重做实验。

只需要把 Proposition 改成两阶段 selection rule。

例如先定义:

$Q_{\max} = \max_{\mathbf{z} \in \mathcal{F}} Q(\mathbf{z}).$

然后定义 $\epsilon$-equivalent set:

$\mathcal{F}_\epsilon = \{\mathbf{z} : Q(\mathbf{z}) \geq Q_{\max} - \epsilon\}.$

最后:

$\mathbf{z}^* = \arg\min_{\mathbf{z} \in \mathcal{F}_\epsilon} R_{\text{texture}}(\mathbf{z}).$

这样含义就是:

> 先找到 fidelity 接近最优的 schedule；在相差不超过 0.1 dB 的 schedule 中，再选择 texture risk 更小的。

那么 late-stage:

$+0.077 \text{ dB} < 0.1 \text{ dB}$

就可以自然被归入 fidelity-equivalent，随后因为 conservative late stage texture risk 更低而选 low。

这时:

$\text{low-high-low}$

才真正由定义严格推出。

甚至标题可以从：

Guardrailed bang–bang optimality

稍微降为：

Guardrailed ε-optimal risk-dominant selection

这样会比现在更严谨。

如果不想再复杂化，我反而建议将：

“conditional optimality guarantee”

改成：

“conditional decision rule”

这也很安全。

我认为这是现在最后一个真正值得担心的技术问题。

<br />

第二个建议:Abstract最好直接突出 276-object holdout，而不是300-object pool
现在abstract中写的是:
C3 is chosen only on a 24-object probe set . and is then transferred unchanged to a 300-objectevaluation pool without re-search.
final\_0903\_with\_letter
虽然现在technically没错，但因为:
24C300,
"transferred to 300-object pool"容易 reviewer 第一眼以为300 是 independent.
正文已经解释得很好:
300 = 24 + 276.
final\_0903\_with letter
所以abstract其实可以写得更强:
"C3 is selected on a 24-object probe set and then evaluated without re-search on the remainingstrictly disjoint 276-object holdout; pooled st J tics over all 300 evaluation objects are alsoreported."

这句话反而比"300-object evaluation pool"更有说服力。
因为Reviewer最关心的不是数量300，而是:
no overlap with tuning
我强烈建议这样改。
checkpoint identifier
\
\
\
第三个问题较小:确认Supplementary 中真的存在exact
回复信明确说:
supplementary material contains a consolidated reproducibility table listing the exact base-
pipeline and adapter checkpoint identifiers.
final 0903\_with\_letter
但是我目前收到的这个final\_0903\_with\_letter.pdf 包含的是 response letter +14页manuscript,本身
没有supplement。
而manuscript主文目前只写:
the adapter checkpoint used for all tables is t' GeoTex-Adapter trained on the 1,118-objecttraining pool . checkpoint and evaluation coae will be made available upon reasonable request.

\
而manuscript主文目前只写:
the adapter checkpoint used for all tables is the GeoTex-Adapter trained on the 1,118-object
training pool .. checkpoint and evaluation code will be made available upon reasonable request.
final\_ 0903\_with letter
所以这里我只能给一个条件性判断:
如果 Supplementary 确实另外上传,而且里面真的有 exact checkpoint ID/ model identifier /
configuration table, 那 Reviewer #1的这一条基本解决。
如果Supplementary实际没有这些内容，那么response letter 和submission package会不一致，这是要
避免的。
我甚建议公开 anonymous code repository 或 supplementary configuration file 会:
available upon reasonable request
更有说服力，但这不是录用的硬性条件。
