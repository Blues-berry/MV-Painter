继续执行 Paper Readiness Audit 后续审计。必须严格按阶段执行，不允许越级。

当前状态：

* Round 01 artifact presence audit: PASS。
* Round 02 evidence traceability audit: FAIL。
* Round 02-Fix: C01 找到替代 metrics path，可以 patch proposal，但尚未修改 registry。
* Round 02-Fix-B: E05 已完成独立统计复现，双运行一致性 PASS。
* E05 recovery 状态：RECOVERABLE_AS_PARTIAL / SUPPORT，不允许标 VERIFIED，不允许作为 MAIN。
* C3 mechanism claim 当前只能写 PARTIAL，不允许写 VERIFIED。
* C3 vs C4 lap_var 有统计支持，但 C3 vs C4 shape metric 为 WARN / borderline，不允许夸大为“无损 shape”。
* 目前仍不允许运行 C3 300-object eval。
* 目前仍不允许开始写论文正文。
* 目前仍不允许修改 final_decision / paper_direction_scorecard 的结论。

总原则：

1. 不训练。
2. 不运行新的 inference。
3. 不修改已有实验结果。
4. 不修改 final_decision / paper_direction_scorecard，除非明确进入最终重建阶段。
5. 不把 C3 标成 VERIFIED。
6. 不把 E05 标成 MAIN。
7. 不把 markdown summary 当作原始证据。
8. 所有新增文件必须放在 paper_readiness_audit 的对应 audit_round_xx 子目录下。
9. 每一轮必须输出 PASS / WARN / FAIL。
10. 如果某一轮 FAIL，立即停止，不进入下一轮。
11. 所有 JSON 必须可 parse。
12. 所有路径必须记录 sha256。
13. 所有结论必须绑定 source path / script / config / metrics / visual / sha256。
14. 不允许用“应该”“可能”“看起来”替代证据链。

目标根目录：

mvpoutput/geotex_refattn_v1/paper_readiness_audit/

============================================================
Round 02-Patch-Proposal: Consolidate Patch Proposal
===================================================

目的：
合并 C01 和 E05 的修复建议，但不修改 evidence_registry。

输入：

* audit_round_02/evidence_traceability_audit.json
* audit_round_02_fix/source_recovery_audit.json
* audit_round_02_fix/proposed_registry_patch.json
* audit_round_02_fix_b/e05_reanalysis.json from run1
* audit_round_02_fix_b/e05_reanalysis.json from run2
* audit_round_02_fix_b/reproducibility_check.json
* audit_round_02_fix_b/e05_recovery_decision.json

输出目录：
mvpoutput/geotex_refattn_v1/paper_readiness_audit/audit_round_02_patch/

只允许新建：

1. consolidated_registry_patch_proposal.json
2. consolidated_registry_patch_proposal.md

要求：

C01:

* 推荐动作为 PATCH_PATH。
* status 最多 PARTIAL。
* paper_use 根据证据链完整性决定 SUPPORT 或 INTERNAL_ONLY。
* replacement_metrics_path 使用实际存在的 extended_scale_sweep_summary.csv。
* 必须记录原错误路径和替代路径。
* 必须记录替代文件 sha256。

E05:

* 推荐动作为 REANALYZE_FROM_RAW_COMPLETED。
* status 只能是 PARTIAL。
* paper_use 只能是 SUPPORT。
* 不允许 MAIN。
* 不允许 VERIFIED。
* 必须记录 verify_C3_mechanism.py path 和 sha256。
* 必须记录 run1/run2 JSON path 和 sha256。
* 必须记录 reproducibility_check.json path 和 sha256。
* 必须记录关键统计：

  * C3 vs C4 lap_var t-stat / p-value
  * C3 vs A_s1p75 lap_var t-stat / p-value
  * C3 vs C4 shape result: WARN / borderline
* notes 必须写明：24-object only，shape side is not fully settled，requires larger validation before MAIN claim。

最终输出：

1. C01 patch proposal ready: YES/NO
2. E05 patch proposal ready: YES/NO
3. 是否实际修改 registry: NO
4. 是否允许 Round 02-Recheck: YES/NO

闸门：

* 如果 C01 或 E05 patch proposal 缺 sha256，判 FAIL，停止。
* 如果 E05 被写成 VERIFIED 或 MAIN，判 FAIL，停止。
* 如果通过，进入 Round 02-Recheck。

============================================================
Round 02-Recheck: Re-run Traceability Logic on Proposed Patch
=============================================================

目的：
不修改原始 registry，只在内存中应用 proposed patch，重新检查 traceability 是否能从 FAIL 变为 WARN/PASS。

输入：

* evidence_registry.json
* consolidated_registry_patch_proposal.json

输出目录：
mvpoutput/geotex_refattn_v1/paper_readiness_audit/audit_round_02_recheck/

只允许新建：

1. patched_traceability_recheck.json
2. patched_traceability_recheck.md

要求：

* 不修改 evidence_registry.json。
* 在脚本内临时应用 patch proposal。
* 重新检查所有 24 条 evidence。
* 重新计算 PASS / WARN / FAIL。
* 特别报告 MAIN evidence 是否还有 FAIL。
* 特别报告 C3 / exploration_v1 相关 evidence 是否仍有 FAIL。
* 特别报告 C01 / E05 修复后状态。

判定规则：
PASS:

* 没有 MAIN evidence FAIL。
* 没有 duplicate evidence_id。
* C01 不再 FAIL。
* E05 不再 FAIL，但必须是 PARTIAL / SUPPORT。
* 整体 FAIL 比例不超过 10%。

WARN:

* 没有 MAIN FAIL，但存在 SUPPORT / APPENDIX evidence FAIL。
* C01/E05 已不阻塞，但仍有辅助路径缺失。

FAIL:

* 任意 MAIN evidence FAIL。
* C01 仍 FAIL。
* E05 仍 FAIL。
* E05 被升级为 MAIN 或 VERIFIED。
* C3 / exploration_v1 仍缺 raw metrics / script / config 任意一项。

最终输出：

1. patched traceability: PASS/WARN/FAIL
2. evidence 总数
3. PASS/WARN/FAIL 数量
4. MAIN evidence FAIL 列表
5. C01 recheck: PASS/WARN/FAIL
6. E05 recheck: PASS/WARN/FAIL
7. 是否允许 Round 03 code trust audit: YES/NO

闸门：

* 如果 FAIL，停止。
* 如果 PASS 或 WARN 且没有 MAIN FAIL，进入 Round 03。

============================================================
Round 03: Code Trust Audit
==========================

目的：
审计关键代码是否可信。只做静态检查和小型只读单元测试，不运行 inference，不训练。

审计对象：

A. scripts/visual_audit/texture_flattening_audit.py
B. geotex/metrics_extended.py
C. geotex/eval_exploration.py
D. geotex/analyze_exploration.py
E. geotex/verify_C3_mechanism.py

输出目录：
mvpoutput/geotex_refattn_v1/paper_readiness_audit/audit_round_03_code_trust/

只允许新建：

1. code_trust_recheck.json
2. code_trust_recheck.md
3. code_unit_test_results.json
4. code_unit_test_results.md

禁止：

* 不运行 inference。
* 不训练。
* 不修改被审计代码。
* 不修 bug。
* 不重写分析结论。

检查内容：

1. py_compile:

* 所有文件必须 py_compile 通过。

2. dangerous pattern scan:
   检查并报告：

* gradient_mean * 0
* shape_gain 阈值不完整
* 重复 if/elif 残留
* hard-coded p-value
* 从 markdown 读取统计结果
* 直接读取 risk_verification.md
* 使用缓存 summary 代替 raw csv
* 未固定 seed 的 bootstrap
* adapter scale 未恢复
* 全局变量污染下一 variant
* object_id 未对齐
* prefix bug，例如 B_ vs B、C_ vs C
* ternary precedence bug

3. metrics_extended.py 单元测试：
   必须构造 synthetic tensor 测试：

* 空 mask
* 全白图
* 有纹理图
* float16 输入
* foreground mask 正常
* Laplacian / HF / gradient 对纹理增强方向正确
* 所有 metric 输出 finite，不允许 nan/inf。

4. verify_C3_mechanism.py 单元测试：

* argparse 必须要求显式输入路径。
* 不允许默认从 markdown 读取。
* object_id mismatch 必须报错。
* 缺 metric column 必须报错。
* bootstrap seed 固定。
* run1/run2 已一致。

5. eval_exploration.py 静态检查：

* timestep-wise scale 是否真的随 denoising timestep 变化。
* layer-wise scale 是否真的按 layer group 绑定。
* variant 结束后是否恢复 adapter scale。
* 是否可能污染下一次 inference。
* 如果无法静态确认，标 WARN，不得标 PASS。

6. analyze_exploration.py 检查：

* PROMISING / METRIC_TRAP 逻辑是否从 raw csv/json 读取。
* 是否存在已经修复但未验证的 prefix/ternary 问题。
* 是否有硬编码 C3 结论。
* 是否有硬编码 p-value。

判定规则：
PASS:

* 所有 py_compile 通过。
* 单元测试通过。
* 没有 dangerous pattern。
* verify_C3_mechanism.py 通过 deterministic checks。
* eval_exploration.py 无明显污染风险。

WARN:

* py_compile 和单元测试通过，但 eval_exploration.py 或 analyze_exploration.py 存在需要人工确认的静态风险。
* 允许继续 Round 04，但 C3 不能升级为 MAIN。

FAIL:

* 任一 py_compile 失败。
* 任一关键单元测试失败。
* verify_C3_mechanism.py 可从 markdown 读数。
* 存在 hard-coded p-value。
* C3 统计脚本不能 object_id 对齐。
* 存在 adapter scale 污染下一 variant 的明确风险。

最终输出：

1. Round 03: PASS/WARN/FAIL
2. 每个文件 verdict
3. 关键风险列表
4. 是否需要修代码: YES/NO
5. 是否允许 Round 04 logic chain audit: YES/NO
6. 是否允许 C3 300-object: NO

闸门：

* FAIL 则停止。
* WARN/PASS 可进入 Round 04。
* 无论 Round 03 结果如何，仍不允许 C3 300-object。

============================================================
Round 04: Logic Chain Audit
===========================

目的：
重建和审计逻辑链，只判断逻辑是否被证据支持，不写论文正文，不给投稿方向最终结论。

输入：

* patched_traceability_recheck.json
* code_trust_recheck.json
* evidence_registry.json
* C01/C05 patch proposal
* E05 reanalysis outputs
* existing logic_chains.json/md

输出目录：
mvpoutput/geotex_refattn_v1/paper_readiness_audit/audit_round_04_logic_chain/

只允许新建：

1. logic_chain_rebuild.json
2. logic_chain_rebuild.md
3. claim_support_matrix.json
4. claim_support_matrix.md

候选逻辑链：

Chain 1: Global scale causes shape/texture trade-off

* Claim: higher global adapter scale improves some shape/foreground metrics but increases texture/detail flattening risk.
* Required evidence:

  * scale sweep metrics
  * visual audit
  * texture metrics or zoom evidence
  * failure cases
* Risk:

  * if texture script was previously flawed, must downgrade.

Chain 2: C3 timestep-wise scaling reduces texture loss vs C4 at same average scale

* Claim: C3 has statistically better texture preservation than C4 under same average scale.
* Required evidence:

  * C3/C4 raw metrics
  * verify_C3_mechanism.py
  * dual-run reproducibility PASS
* Required limitation:

  * 24-object only
  * shape side borderline
  * SUPPORT only, not MAIN.

Chain 3: C3 is a promising candidate, not validated final method

* Claim: C3 is promising for larger validation.
* Required evidence:

  * E05 PARTIAL
  * no 300-object C3 yet
* Limitation:

  * not paper-ready as main method.

Chain 4: v2a continue-control is negative evidence

* Claim: continuing training did not solve scale/internalization reliably.
* Required evidence:

  * 50-object eval
  * 300-object eval
  * stop decision
* Use:

  * SUPPORT / appendix / negative result.

Chain 5: attn2-only / RAIS old direction is appendix/support

* Claim: reference-preservation diagnosis exists but not enough as current main contribution.
* Required evidence:

  * lora.pdf / RAIS plots / old eval
* Use:

  * appendix or related diagnostic only.

每条 claim 输出：

* claim_id
* claim_text
* support_level: STRONG / MODERATE / WEAK / UNSUPPORTED
* required_evidence
* available_evidence
* missing_evidence
* conflicting_evidence
* current_allowed_use: MAIN / SUPPORT / APPENDIX / INTERNAL_ONLY / DELETE
* wording_allowed
* wording_forbidden

特别要求：

* 禁止写 “C3 verified final method”。
* 禁止写 “C3 solves shape-texture tradeoff”。
* 允许写 “C3 is a reproducible 24-object candidate that preserves texture better than C4 at the same average scale, while shape effects remain borderline.”
* 禁止写 “s=2.50 visual passed”。
* 允许写 “s=2.50 is metric-strong but visually risky.”

判定规则：
PASS:

* 所有 claim 都有明确 support_level。
* 没有 unsupported claim 被标 MAIN。
* C3 被限制为 SUPPORT/CANDIDATE。
* wording_allowed / wording_forbidden 完整。

WARN:

* 有 claim 证据不足但已降级。
* 逻辑链可继续用于规划，但不能开始写论文。

FAIL:

* unsupported claim 被标 MAIN。
* C3 被标 VERIFIED 或 final method。
* 旧 attn2-only 被恢复为主线且没有新证据。
* s=2.50 被写成 visual passed。

最终输出：

1. Round 04: PASS/WARN/FAIL
2. 哪些 claim 可作为 MAIN
3. 哪些 claim 只能 SUPPORT
4. 哪些 claim 只能 APPENDIX
5. 哪些 claim 必须删除
6. C3 当前允许表述
7. 是否允许 Round 05 paper direction re-score: YES/NO
8. 是否允许 C3 300-object: NO

闸门：

* FAIL 则停止。
* PASS/WARN 可进入 Round 05。
* 仍不允许 C3 300-object。

============================================================
Round 05: Paper Direction Re-score
==================================

目的：
基于修复后的证据链重新评分论文方向。不是写论文，只做 go/no-go/hold 判断。

输入：

* Round 02-Recheck
* Round 03 Code Trust
* Round 04 Logic Chain
* existing paper_direction_scorecard.json/md

输出目录：
mvpoutput/geotex_refattn_v1/paper_readiness_audit/audit_round_05_direction_rescore/

只允许新建：

1. direction_rescore.json
2. direction_rescore.md
3. gap_to_go_checklist.json
4. gap_to_go_checklist.md

候选方向：
A. Timestep-conditioned adapter scaling
B. Shape-texture trade-off diagnosis
C. Practical 5090 LoRA adaptation for MV-Painter
D. Reference-attention-safe LoRA / attn2-only
E. Combined: diagnosing and controlling adapter strength in reference-guided multi-view diffusion

评分维度，每项 1-5：

* novelty
* technical_depth
* experimental_strength
* visual_persuasiveness
* reproducibility
* reviewer_defensibility
* additional_work_required
* risk_level

每个方向必须给：

* score_total
* current_status: GO / BORDERLINE / HOLD / NO_GO
* strongest_evidence
* weakest_evidence
* fatal_gap
* minimum_next_experiments
* whether_requires_300obj
* whether_requires_new_training
* whether_can_write_now

强制规则：

* C3 相关方向如果没有 300-object validation，不能 GO，最多 BORDERLINE/HOLD。
* attn2-only 旧方向不能 GO，除非有新的 task adaptation 证据。
* 5090 LoRA 工程方向不能作为主论文方向，最多 SUPPORT。
* Combined 方向如果主 claim 依赖 C3，则必须标 BORDERLINE/HOLD。
* 如果所有主方向都依赖 C3 300-object，则 conclusion 必须是 “audit first, then validation”，不是 “start writing”.

最终输出：

1. Round 05: PASS/WARN/FAIL
2. 最强方向
3. 第二方向
4. GO/BORDERLINE/HOLD/NO_GO
5. 是否现在可以开始写论文: YES/NO
6. 最少还缺哪 3 个实验
7. 是否允许 Round 06 final decision rebuild: YES/NO
8. 是否允许 C3 300-object: 只允许写 PREPARE_PLAN，不允许 RUN

闸门：

* FAIL 停止。
* PASS/WARN 进入 Round 06。
* 仍不直接跑 300-object。

============================================================
Round 06: Final Decision Rebuild
================================

目的：
重建 final decision，但必须基于 Round 02–05 审计结果。不能复用旧 final_decision。

输入：

* Round 02-Recheck
* Round 03 Code Trust
* Round 04 Logic Chain
* Round 05 Direction Rescore

输出目录：
mvpoutput/geotex_refattn_v1/paper_readiness_audit/audit_round_06_final_rebuild/

只允许新建：

1. final_decision_rebuilt.json
2. final_decision_rebuilt.md
3. next_experiment_plan.json
4. next_experiment_plan.md

必须回答：

1. 当前是否达到论文推进条件：

   * YES / BORDERLINE / NO
2. 最强论文方向是什么
3. 第二候选方向是什么
4. 哪些旧方向废弃
5. 哪些结果只能 appendix
6. 哪些结果必须重跑验证
7. 哪些代码必须修复
8. 还缺哪 3 个关键实验
9. 下一步应该继续 audit / 继续大规模验证 / 开始写论文
10. 如果推进论文，建议题目和核心贡献一句话

强制限制：

* 如果 C3 未做 300-object，不能写 YES，最多 BORDERLINE。
* 如果 Round 03 是 WARN，不能写“代码完全可信”。
* 如果 Round 04 中 C3 是 SUPPORT，不能把 C3 写成主贡献。
* 如果 final decision 依赖 C3，必须写 “requires 300-object validation before paper writing.”
* 不允许写“唯一 blocker 是 C3 300-object”，除非其他所有审计均 PASS。
* 不允许写“C3 default inference config”，只能写“candidate for larger validation”。

next_experiment_plan 必须只列 1-3 个实验，不允许超过 3 个：
候选实验：

1. C3 300-object validation preflight + run
2. C3 visual comparison panel vs s=1.25 / s=2.00 / s=2.50 / C4
3. C3 ablation on timestep boundary / scale values

注意：
Round 06 只生成 plan，不运行实验。

最终输出：

1. final decision: YES/BORDERLINE/NO
2. strongest direction
3. second direction
4. 是否允许写论文: YES/NO
5. 是否允许准备 C3 300-object: YES/NO
6. 是否允许直接运行 C3 300-object: YES/NO
7. next experiments 1-3
8. stop condition

闸门：

* Round 06 完成后停止。
* 不运行 C3 300-object。
* 等用户确认后再进入实验阶段。

============================================================
Global Stop Rules
=================

任何阶段出现以下情况，立即停止并输出 FAIL：

1. 修改了 evidence_registry.json / .md。
2. 修改了 final_decision 或 paper_direction_scorecard 旧文件。
3. 运行了 inference。
4. 运行了训练。
5. 把 C3 标成 VERIFIED。
6. 把 E05 标成 MAIN。
7. 用 markdown summary 代替 raw metrics。
8. 发现 MAIN evidence FAIL 但继续进入下一轮。
9. 发现代码审计 FAIL 但继续评分论文方向。
10. 生成超过指定数量的输出文件。
11. 输出论文正文。
12. 宣称“可以投稿”但没有 300-object C3 或等价主实验验证。

最终总输出格式：

每完成一轮，只输出：

* Round name
* PASS/WARN/FAIL
* 创建了哪些文件
* 关键发现 3 条以内
* 是否进入下一轮：YES/NO
* 如果 NO，停止原因

不要输出长篇总结。
不要写论文方向宣传。
不要写论文正文。
